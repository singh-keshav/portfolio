import React from 'react'
import Breadcrumb from '../../components-ui/Breadcrum'

type Props = {
  items: { label: string; href?: string }[];
}

const BreadcrumBar = (props: Props) => {
  return (
    <div className='bg-gray-900 mb-4 py-4'>
      
    </div>
  )
}

export default BreadcrumBar